{
  "name": "wallet-app",
  "private": true,
  "version": "1.0.0",
  "description": "Butterflies in the wallet — PWA offline per entrate e uscite personali",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"tests/*.test.js\""
  },
  "devDependencies": {
    "vite": "^8.3.1"
  }
}
